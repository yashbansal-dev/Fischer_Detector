{
  "name": "fischer-detector-root",
  "private": true,
  "workspaces": [
    "project/web"
  ],
  "scripts": {
    "build": "npm run build --workspace=web && cp -r project/web/dist .",
    "dev": "npm run dev --workspace=web"
  }
}
